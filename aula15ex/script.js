let n = [2, 4, 5, 6, 7]
n.indexOf(7)
console.log(n)
    for( let pos in n) {
        console.log(`A posição ${pos} tem os valores ${n[pos]}`)
    }

// for(let pos=0; pos<n.length; pos++ ) { 
    // console.log(`A posição é ${pos} ${n[pos]} `)

// }